# Production decisions

- Active plan CODED-DEFINITIVE-20260906-v1, PR124 head 90bb4a9d3cfc96a4d4715abb44aeec74f4e1e46e; original plan copies verified by Git blob SHA.
- Owner authorization received directly in this Astra task from attachment 4e089291-2fa0-49ae-8ea0-82eb343ab640 on 2026-09-06. Necessary Three.js/types/lockfile and mesh runtime formats are approved. The former protected setup hold is resolved by that owner message, not by inference from the plan.
- Source project AGENTS.md is generated/read-only. It remains unchanged; this branch's AGENTS.md records current authority. Shared ChatGPT project setting awaits the owner's browser sign-in. No security/permission settings are changed.
- Work occurs in a fresh sibling worktree on astra/definitive-review, starting at main 6743368. PR122 and all existing branches remain intact.
- Finish and test connected Earth-to-Mars before later-world expansion. Preserve legacy entry routes and old saves for comparison.
- Existing Windows source validators require LF; add scoped text attributes to prevent false failures on future checkouts.
- Runtime dependency: Three.js 0.185.1 with development types @types/three 0.185.4, matching revision 185. Necessary for real mesh rendering, glTF skins/animations, camera attachments, and GPU resource accounting. The renderer is dynamically imported when a 3D route is entered. Implementation follows the official [GLTFLoader](https://threejs.org/docs/pages/GLTFLoader.html) and [color-management](https://threejs.org/manual/en/color-management.html) documentation.
- Windows reproducibility: the starting lockfile omitted @rollup/rollup-win32-x64-msvc despite Rollup 4.62.2 requiring it. Even npm ci --include=optional omitted the missing lock entry. Add that exact 4.62.2 package as an optional platform dependency; a subsequent ordinary npm ci installs it and builds successfully. No unrelated package versions were upgraded.
- Saves: coded-xrp-definitive-v1:campaign and separately named test slots. Existing v1/v2/v3 campaign keys are migration inputs only. Preserve unknown/future/corrupt saves; fail a transaction before mutating live state when persistence fails. No browser save is overwritten by restoring a code backup.
- Keep PR108/107/111/113/114/117/118 attribution. The combined campaign path needed four additional escort corrections, each proved by a failing mutation. See SALVAGE_LEDGER.md.
- Foundation mesh derivative: join matching material surfaces in the accepted v03 LOD2 without changing attachment transforms. The resulting 396,960-byte GLB has 5,156 triangles and six surfaces. Actual consumer is ?review=model. Materials remain provisional; this is not final Warship art or playable mesh transit.
- Single manifest now distinguishes models/audio from images, and the real 2D loader skips non-image types. Existing separate audio track configuration and legacy image scene groups still need consolidation in the full asset pass.
- Automatic approval review rejected the first PR121 start payload because it contained internal backup/master verification details. A reduced plan-and-scope-only notice was subsequently approved and posted. Keep public milestones limited to relevant implementation evidence and share-safe original renders.
