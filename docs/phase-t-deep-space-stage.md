# Phase T — Deep-space lane

Phase T consumes the one original vertical background that was not used by Phase P.

- Adds the optimized `bg_deep_space_lane_vertical` master as a 720 × 1280 runtime WebP.
- Unlocks Deep Space Lane at wave 11, between the regulatory approach and the late bosses.
- Uses a faster scroll speed and a clean corridor without decorative ground structures.
- Does not change score thresholds, campaign length, bosses, enemy balance, or collision.
